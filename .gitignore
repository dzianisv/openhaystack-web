.env
firmware/src/nrf-sdk/
firmware/src/release/

# Secrets: tracker private keys (see README) must never be committed
trackers.json
tracker.json
*.key

# Secrets: FindMy.py session state (Apple ID account + anisette) must never be committed
account.json
ani_libs.bin
anisette-libs.bin
*.anisette
*.anisette.json
anisette.json

# Local environment / build artifacts
.venv/
__pycache__/
*.pyc
